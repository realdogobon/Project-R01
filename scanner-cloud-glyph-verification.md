# Scanner Cloud Glyph Refinement — Visual Verification

**Date:** 15 August 2026

The scanner’s idle cloud outline now uses a restrained, low-saturation light blue rather than neutral gray. The light-theme capture shows a clear but soft blue mark against the white document canvas; the dark-theme capture uses a slightly lighter blue at reduced opacity to preserve contrast without becoming an accent block.

The dedicated browser probe confirmed that actual stored light and dark application themes were applied, with distinct computed cloud colors in each. The focused upload-state probe also passed for idle, hover, drag, selected, pending, success, settled preview, and silent failure. The hover comparison measured the same transparent background, no box shadow, and no transform before and after hover.

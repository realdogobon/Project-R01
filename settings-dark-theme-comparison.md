# Settings dark-theme comparison

The focused desktop comparison was captured after the nested-view spacing and Scanner copy changes. The Settings rail keeps its compact glyph geometry, the active orange accent remains visible without overpowering the dark surface, and the Appearance rows have clear separation beneath the title divider. The Scanner surface keeps readable white labels, muted helper text, sufficiently distinct dark input fields, and the key glyph remains aligned with the active category indicator.

The desktop comparison completed with zero browser errors. The remaining mobile captures are part of the same verification harness and are checked separately before the checkpoint is saved.

The mobile Scanner capture preserves the rail, active key glyph, helper copy, and all three fields without horizontal clipping. The initial mobile Sound Centre image was captured during the old exit-only transition state; the transition was corrected, and a subsequent DOM probe confirmed the Sound Options body renders. The final desktop dark capture shows the Sound Options heading plus Keyboard Sounds and Error Sounds rows with visible breathing room beneath the title divider.

The final desktop Scanner capture keeps the new copy legible and restrained: “Add a key to use cloud scanning. Without one, scans stay on this device.” The key fields remain evenly stacked and the active key glyph is aligned with the rail indicator.
